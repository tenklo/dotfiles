#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

export EDITOR="vim"
export BROWSER="firefox"

#if [[ "$(tty)" == "/dev/tty2" ]]; then
#        pgrep i3 || startx
#else
echo "Welcome back, master."
#fi
